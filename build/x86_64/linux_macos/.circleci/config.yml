version: 2
parallelism: 8
jobs:
  build:
    docker:
      - image: graal-jdk8:latest
    steps:
      - checkout
      - run:
          name: Build via the docker build script
          command: ./build/x86_64/linux_macos/docker-build.sh